package algorithms.trie;

import datastructures.types.Trie;

import java.util.List;

/**
 * Given an array of strings words[] and a partial string str, the task is to find the strings of the given
 * form str from the given array of string.
 * <p>
 * A partial string is a string with some missing characters. For Example: “..ta”, is a string of length 4
 * ending with “ta” and having two missing character at index 0 and 1.
 */
//public class PatternMatching {
//
//    public static void main(String[] args) {
//
//    }
//
//    public List<String> search(String[] list, String pattern) {
//        Trie trie = new Trie();
//        trie.insert(list);
//        int len = pattern.length();
//
//        for(char c : pattern.toCharArray()) {
//            if(c == '.') {
//
//            } else {
//
//            }
//        }
//    }
//
//}
